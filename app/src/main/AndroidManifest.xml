<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muradesal.last_time_proj">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ttt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Patient.Patient_after_home"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Patient.MainActivity"
            android:launchMode="singleTop">

        </activity>

        <activity
            android:name=".ADMEN.adme_page"
            android:launchMode="singleTop">


        </activity>
        <activity
            android:name=".STTAF.emp_home_page"
            android:launchMode="singleTop">


        </activity>
        <activity
            android:name=".HOSIPTAL.hospital_home_page"
            android:launchMode="singleTop" />
        <activity
            android:name=".HOSIPTAL.hospital_note"
            android:launchMode="singleTop" />
        <activity
            android:name=".HOSIPTAL.hospital_registration"
            android:launchMode="singleTop" />
        <activity
            android:name=".Patient.phone_auth"
            android:launchMode="singleTop" />
        <activity
            android:name=".HOSIPTAL.hopital_patient_bik"
            android:launchMode="singleTop" />
        <activity
            android:name=".Patient.Patient_info_send"
            android:launchMode="singleTop" />
        <activity
            android:name=".HOSIPTAL.hospital_notfi"
            android:launchMode="singleTop" />
        <activity
            android:name=".STTAF.employes_page"
            android:launchMode="singleTop" />
        <activity
            android:name=".HOSIPTAL.hospital_emp_rigst"
            android:launchMode="singleTop" />

        <activity
            android:name=".Patient.blood_search"
            android:launchMode="singleTop" />
        <activity
            android:name=".Patient.second_aid"
            android:launchMode="singleTop" />
        <activity
            android:name=".Patient.first_aid"
            android:launchMode="singleTop" />

        <service android:name=".firebasemessingsend">
            <intent-filter>
                <action android:name="com.google.firebase.INSTACE_ID_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_valu" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".Patient.Patient_registration"
            android:launchMode="singleTop" />

        <activity
            android:name=".HOSIPTAL.hospit_bik_pat"
            android:launchMode="singleTop" />
        <activity
            android:name=".ADMEN.admen_add"
            android:launchMode="singleTop" />

        <activity android:name=".ADMEN.dises" />
        <activity android:name=".ADMEN.splach" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Patient.MapsActivity_place"
            android:label="@string/title_activity_maps_place" />
        <activity android:name=".ADMEN.place_mark_add" />
        <activity android:name=".HOSIPTAL.hospital_list_info" />
        <activity android:name=".HOSIPTAL.hospital_sector" />

    </application>

</manifest>