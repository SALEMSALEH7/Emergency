<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="18dp"
    android:paddingRight="18dp"
    tools:context=".HOSIPTAL.hospital_registration">


    <EditText
        android:id="@+id/emile_hos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="الايميل"
        android:inputType="textEmailAddress" />

    <EditText
        android:id="@+id/name_hos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="اسم المستشفى"
        android:inputType="text"


        />
    <EditText
        android:id="@+id/location_hospi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="موقعها"
        android:inputType="text"
        />

    <EditText
        android:id="@+id/pass_hospital"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="الرقم السري"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/adding_it"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:textColor="@color/red_primary"
        android:background="@drawable/log_in_shape"
        android:text="تسجيل"
        />



</LinearLayout>